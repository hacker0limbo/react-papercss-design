import { Alert } from 'react-papercss-design';

export default () => {
  return (
    <>
      <Alert type="primary" dismissible closeLabel="Close Me">
        Alert-primary
      </Alert>
    </>
  );
};
